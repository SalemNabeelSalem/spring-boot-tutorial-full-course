server:
  error:
    include-message: always
  port: 8008

spring:
  datasource:
    url: jdbc:h2:mem:schooldb
    driver-class-name: org.h2.Driver
    username: salem_nabeel
    password: s_n
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      path: /h2-ui # http://localhost:8008/h2-ui